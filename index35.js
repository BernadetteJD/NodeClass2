var clientId = 143245;
var inputClientId = '143245';

console.log(clientId == inputClientId); // true the values are the same
console.log(clientId === inputClientId); // false because the data types is that the same
